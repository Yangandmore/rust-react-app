import { mainAction, mainReducer, mainSelect } from './main';
import { dictAction, dictReducer, dictSelect } from './dict';
import { roleAction, roleReducer, roleSelect } from './role';

export { mainAction, mainReducer, mainSelect, dictAction, dictReducer, dictSelect, roleAction, roleReducer, roleSelect };
